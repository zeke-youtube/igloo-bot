services:
  igloobot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: igloobot
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - igloobot_data:/app/src/data

volumes:
  igloobot_data: