const { SlashCommandBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle, EmbedBuilder } = require('discord.js'); const config = require('../config'); const oauth = require('../oauth');
module.exports = { data: new SlashCommandBuilder().setName('addperms').setDescription('Connect your Discord account for PikaPeng Travel.'), async execute(i) { if (!config.oauthClientSecret() || !config.oauthRedirectUri()) return i.reply({ content: `${config.pengEmoji()} OAuth is not configured yet. Add OAUTH_CLIENT_SECRET and OAUTH_REDIRECT_URI to the bot environment.`, ephemeral: true }); const url = oauth.authUrl(i.user.id); return i.reply({ embeds: [new EmbedBuilder().setColor(0x6bd6e8).setTitle(`${config.pengEmoji()} Connect Discord for Travel`).setDescription('Authorize PikaPeng to verify your server memberships and join you to approved destinations after you confirm a trip.\n\nRequested: identify, guild list, and guild joining.')], components: [new ActionRowBuilder().addComponents(new ButtonBuilder().setLabel('Authorize Discord').setURL(url).setStyle(ButtonStyle.Link))], ephemeral: true }); } };
