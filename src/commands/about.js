const { SlashCommandBuilder, EmbedBuilder } = require('discord.js');
const config = require('../config');
module.exports = { data: new SlashCommandBuilder().setName('about').setDescription('Learn about PengBot and PikaStudio.'), async execute(interaction) { const emoji = config.pengEmoji(); const embed = new EmbedBuilder().setColor(0x6bd6e8).setTitle(`${emoji} About PengBot`).setDescription('PengBot is the official PikaPeng companion for the PikaStudio Discord server.').addFields({ name: 'Built for PikaStudio', value: 'Helpful server tools, polished announcements, and a little penguin personality.' }, { name: 'Commands', value: '`/about` · `/pengfact` · `/pikastudiosites` · `/announcements`' }, { name: 'Version', value: 'PengBot 1.0.0' }).setFooter({ text: 'PengBot · PikaStudio' }); return interaction.reply({ embeds: [embed] }); } };
