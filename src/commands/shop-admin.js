const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js'); const shops = require('../shops'); const config = require('../config');
function command(name, action, description) { return { data: new SlashCommandBuilder().setName(name).setDescription(description).addStringOption(o => o.setName('shop').setDescription('Shop name or ID').setRequired(true)).addStringOption(o => o.setName('reason').setDescription('Reason').setRequired(true).setMaxLength(500)), async execute(i) { if (!i.memberPermissions?.has(PermissionFlagsBits.Administrator)) return i.reply({ content: `${config.pengEmoji()} Server Administrator permission is required.`, ephemeral: true }); const r = await shops.moderate(i.guildId, i.options.getString('shop'), i.user.id, action, i.options.getString('reason')); return i.reply({ content: r.error ? `${config.pengEmoji()} Shop not found or cannot be changed.` : `🏪 Shop ${action}d. Pending refunds: ${r.refunded || 0} 🐟.`, ephemeral: true }); } }; }
module.exports = { remove: command('removeshop', 'remove', 'Remove a shop and refund pending orders.'), suspend: command('suspendshop', 'suspend', 'Suspend a shop.'), restore: command('restoreshop', 'restore', 'Restore a suspended shop.') };
