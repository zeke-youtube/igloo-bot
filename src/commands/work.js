const { SlashCommandBuilder, EmbedBuilder } = require('discord.js');
const config = require('../config');
const work = require('../work');
module.exports = { data: new SlashCommandBuilder().setName('work').setDescription('Perform a random PikaPeng job for Fish.'), async execute(i) { if (i.user.bot) return i.reply({ content: `${config.pengEmoji()} Bots cannot work shifts.`, ephemeral: true }); const result = await work.performWork(i.user.id); if (result.onCooldown) return i.reply({ content: `🐧 You're still recovering from your last shift.\n\nYou can work again <t:${Math.ceil(result.nextWorkAt / 1000)}:R>.`, ephemeral: true }); const labels = { normal: '🛠️ SHIFT COMPLETE', great: '✨ EXCELLENT WORK!', failure: '💥 SHIFT FAILED', rare: '🏆 EMPLOYEE OF THE MINUTE' }; const notes = { normal: 'PikaPeng inspected your work and somehow approved it.', great: 'Nobody is admitting who unplugged the cable.', failure: 'The shift was a disaster, but at least it was entertaining.', rare: 'This result has been added to the IglooBot incident archives.' }; const embed = new EmbedBuilder().setColor(result.outcome === 'failure' ? 0xf4c95d : 0x6bd6e8).setTitle(labels[result.outcome]).setDescription(`${result.message}\n\n${notes[result.outcome]}`).addFields({ name: '🛠️ Job', value: result.job.title, inline: true }, { name: '🐟 Earnings', value: `+${result.reward} fish`, inline: true }, { name: '💰 Balance', value: `${result.balance} fish`, inline: true }).setFooter({ text: `Next shift <t:${Math.ceil(result.nextWorkAt / 1000)}:R> · PengBot · PikaStudio` }); return i.reply({ embeds: [embed] }); } };
