const { SlashCommandBuilder, ModalBuilder, TextInputBuilder, TextInputStyle, ActionRowBuilder } = require('discord.js');
module.exports = { data: new SlashCommandBuilder().setName('announcements').setDescription('Create a PikaStudio announcement (staff only).'), async execute(i) { const modal = new ModalBuilder().setCustomId('announcement_modal').setTitle('New PikaStudio announcement'); const fields = [['announcement_title', 'Title', TextInputStyle.Short, true], ['announcement_content', 'Content', TextInputStyle.Paragraph, true], ['announcement_link', 'Optional link', TextInputStyle.Short, false], ['announcement_footer', 'Optional footer', TextInputStyle.Short, false]]; modal.addComponents(...fields.map(([id, label, style, required]) => new ActionRowBuilder().addComponents(new TextInputBuilder().setCustomId(id).setLabel(label).setStyle(style).setRequired(required).setMaxLength(style === TextInputStyle.Paragraph ? 4000 : 256)))); return i.showModal(modal); } };
