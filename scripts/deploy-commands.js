const { REST, Routes } = require('discord.js'); const config = require('../src/config'); const commands = ['about', 'help', 'pengfact', 'pikastudiosites', 'announcements', 'coinflip', 'clear', 'global-cooldown', 'createroom', 'doorbell', 'ktvmanage', 'fish', 'balance', 'givefish', 'gamble', 'profile', 'waddle'].map((n) => require(`../src/commands/${n}`).data.toJSON());
try { config.validateEnv(); } catch (error) { console.error(`Deployment configuration error: ${error.message}`); process.exit(1); }
(async () => { const rest = new REST({ version: '10' }).setToken(config.token()); await rest.put(Routes.applicationGuildCommands(config.clientId(), config.guildId()), { body: commands }); console.log(`Deployed ${commands.length} PengBot commands.`); })().catch((e) => { console.error(`Discord command deployment failed: ${e.message}`); process.exitCode = 1; });
